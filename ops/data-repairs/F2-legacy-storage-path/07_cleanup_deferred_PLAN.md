# 07 — خطة Cleanup (مؤجّلة) — خاصة بـF2 حصريًا. لا تُنفَّذ الآن.

**نطاق Batch 2 يقتصر على:** السجلات السبعة + الملفات الأربعة الفريدة + النسخ القديمة في `/1/0/document/`.
**لا حذف لأي مصدر ضمن Batch 2.** هذه الخطة لِما بعده فقط.

## ما يشمله هذا التنظيف (F2 فقط)
النسخ القديمة الأصلية للملفات الأربعة الفريدة الباقية في `/app/uploads/1/0/document/`
(لأننا استخدمنا **Copy لا Move**، فبقيت في مكانها).

## شروط بدء التنظيف (كلها إلزامية)
1. `06_download_and_perms_test.sh` أخضر 7/7.
2. مرور نافذة احتفاظ متفق عليها (مقترح 30 يومًا) بعد نجاح الترحيل.
3. نسخة احتياطية مؤكَّدة تشمل الوجهة الجديدة (`restore-verify` أخضر).
4. موافقة صريحة منفصلة من المالك.

## قاعدة الحذف الآمن (لكل ملف من الأربعة)
يُحذف مصدر `/1/0/document/<f>` **فقط** إذا:
- لا سجل (df/dr/ca) في DB ما يزال يشير إليه (أعد فحص "SHARED SOURCE" ومطابقة أسماء الملفات).
- نظيره في `/1/1/document/<f>` نُزِّل بنجاح منذ ≥ النافذة.
- الحذف صفًّا صفًّا عبر `deleteStoredObject` (يحترم الحاويات ومنع traversal) أو `rm` مُدقَّق يدويًا. **ممنوع** `rm -rf` جماعي.

## خارج النطاق صراحةً (Batch مستقل وقرار منفصل)
تنظيف بيانات UAT (project 15، doc 65، file 7، صفوف correspondence/F10، وأي بيانات UAT أخرى)
**ليس جزءًا من F2 ولا من هذه الخطة.** يُدار كـBatch مستقل بقراره الخاص.
